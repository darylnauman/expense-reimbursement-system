server:
  port: 7500
management:
  endpoints:
    web:
      exposure:
        include: "*"
spring:
  #datasource:
    #username: root
    #password: p4ssw0rd
    #url: jdbc:mysql://34.135.95.88:3306/my-docker-database
    #driverClassName: com.mysql.cj.jdbc.Driver
  #jpa:
    #hibernate:
      #ddl-auto: update
      #naming:
        #physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    #show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: darylnauman@gmail.com
    password: ${GMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

    #props:
     # mail.smtp.auth: true
      #mail.smtp.starttls.enable: true
      #mail.smtp.ssl.trust: <host>
      #mail.smtp.host: smtp.gmail.com
      #mail.smtp.port: 587
